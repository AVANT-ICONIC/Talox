name: Apply cross-origin frame navigation race guard

on:
  push:
    branches:
      - fix/cross-origin-active-page

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/cross-origin-active-page
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Apply asserted patch
        run: node scripts/apply-cross-origin-frame-navigation-race.mjs
      - name: Remove one-shot patch machinery
        run: rm scripts/apply-cross-origin-frame-navigation-race.mjs .github/workflows/apply-cross-origin-frame-navigation-race.yml
      - name: Validate diff and commit
        run: |
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/core/CrossOriginManager.ts tests/unit/CrossOriginManagerRebindBootstrap.test.ts scripts/apply-cross-origin-frame-navigation-race.mjs .github/workflows/apply-cross-origin-frame-navigation-race.yml
          git commit -m "fix: reject stale cross-origin sessions after frame navigation"
          git push origin HEAD:fix/cross-origin-active-page
